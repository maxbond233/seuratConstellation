% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_constellation_feature.R
\name{plot_constellation_feature}
\alias{plot_constellation_feature}
\title{Plot Constellation Feature}
\usage{
plot_constellation_feature(
  knn_graph,
  seu,
  feature,
  assay = NULL,
  layer = "data",
  feature_stat = c("avg", "median", "pct"),
  pct_threshold = 0,
  node.label = "cluster_label",
  exxageration = 2,
  curved = TRUE,
  node.dodge = FALSE,
  label.size = 5,
  max_size = 10,
  label_repel = FALSE,
  node_trans = "sqrt",
  hull_type = "none",
  hull_alpha = 0.2,
  hull_expand = 0.1
)
}
\arguments{
\item{knn_graph}{A constellation_knn object with cluster centers computed}

\item{seu}{A Seurat object used to compute feature expression}

\item{feature}{Character. Gene/feature name to visualize}

\item{assay}{Character. Assay to use (defaults to DefaultAssay)}

\item{layer}{Character. Layer to use (default "data"). For Seurat V5 compatibility.}

\item{feature_stat}{Character. "avg", "median", or "pct" (percent expressing)}

\item{pct_threshold}{Numeric. Threshold for percent expressing (default 0)}

\item{node.label}{Character. Column name for node labels. Default "cluster_label"}

\item{exxageration}{Numeric. Edge width exaggeration factor. Default 2}

\item{curved}{Logical. Whether edges should be curved. Default TRUE}

\item{node.dodge}{Logical. Whether to dodge overlapping nodes. Default FALSE}

\item{label.size}{Numeric. Size of node labels. Default 5}

\item{max_size}{Numeric. Maximum node size. Default 10}

\item{label_repel}{Logical. Use ggrepel for labels. Default FALSE}

\item{node_trans}{Character. Size transformation. Default "sqrt"}

\item{hull_type}{Character. Hull type: "none", "convex", or "concave". Default "none"}

\item{hull_alpha}{Numeric. Hull transparency (0-1). Default 0.2}

\item{hull_expand}{Numeric. Expansion factor for hulls. Default 0.1}
}
\value{
A ggplot object
}
\description{
Plot a constellation graph with nodes colored by gene expression

Creates a constellation plot where each node is colored by
cluster-level expression of a feature (e.g., average expression or percent
expressing). Intended to be used after build_knn_graph(),
filter_knn_edges(), and compute_cluster_centers().
}
\examples{
\dontrun{
seu \%>\%
  build_knn_graph(cluster_col = "celltype") \%>\%
  filter_knn_edges(frac_th = 0.05) \%>\%
  compute_cluster_centers() \%>\%
  plot_constellation_feature(seu, feature = "MS4A1")
}
}
